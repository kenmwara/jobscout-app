node_modules/
.wrangler/
.dev.vars

# native builds (generated in CI, never committed)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/gradle/
android/gradlew*
ios/JobScout.xcodeproj/
ios/Info.plist
ios/build/

# Kotlin compiler scratch — build noise, never source
android/.kotlin/

# handover stills are shipped as a zip to the operator, not kept in git
docs/handover/**/*.png
